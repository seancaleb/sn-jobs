import Jobs, { loader as jobsLoader } from "./Jobs.page";
import Job, { loader as jobLoader } from "./Job/Job.page";
import JobApplication, {
  loader as jobApplicationLoader,
} from "./JobApplication/JobApplication.page";

export { Jobs, Job, JobApplication, jobsLoader, jobLoader, jobApplicationLoader };
