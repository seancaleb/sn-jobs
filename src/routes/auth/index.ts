import Login, { loader as loginLoader } from "./Login/Login.page";
import Register, { loader as registerLoader } from "./Register/Register.page";

export { Login, loginLoader, Register, registerLoader };
